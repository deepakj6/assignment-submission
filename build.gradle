buildscript {
    repositories {
        maven { url "http://platform-acceleration-lab-maven.s3.amazonaws.com" }
        jcenter()
    }

    dependencies {
        classpath "io.pivotal.pal.assignments:assignments-plugin:3.5.2"
    }
}

apply plugin: "io.pivotal.pal.assignments.plugin"

assignments {
    apiUrl = "https://assignments.education.pivotal.io"
    emails = [
       "kanhu.behera@wipro.com", // update me
       
    ]
    cohortIdentifier = 349803361 // update me
}

defaultTasks "tasks"
